spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
  flyway:
    enabled: true
  rabbitmq:
    listener:
      simple:
        auto-startup: false

logging:
  level:
    root: WARN
    com.example.springjpa: WARN

jwt:
  secret: test-secret-key-for-jwt-signing-that-is-long-enough
  expiration-ms: 86400000
